import React from 'react';

const Join = (props) => (
    <section className='content'>
        <div className="inner">
            <h1>JOIN</h1>
        </div>
    </section>
);

export default Join;